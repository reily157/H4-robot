[pytest]
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
testpaths = tests
addopts = -m "not slow"
markers =
    slow: live mainnet tests (≥60s, require network) — run with: pytest -m slow
